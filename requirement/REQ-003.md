# REQ-003: Phân Tích Cấu Trúc Kiểu Gõ (Input Method) và Đánh Giá Ảnh Hưởng Khi Xóa

**Phiên bản:** 1.0  
**Ngày cập nhật:** 2026-08-16  
**Ngôn ngữ:** Tiếng Việt  
**Mục đích:** Hiểu rõ cấu trúc các kiểu gõ trong project để quyết định xóa những kiểu không dùng (chỉ giữ Telex).

---

## 1. Tổng Quan Kiến Trúc

Dự án chia làm 3 tầng:

```mermaid
flowchart TB
    subgraph Layer1["Tầng 1: Go Core (bamboo-core)"]
        A1["input_method_def.go"]
        A2["bamboo.go / bamboo-c.go"]
        A3["rules_parser.go"]
        A4["bamboo-core.a (build output)"]
    end

    subgraph Layer2["Tầng 2: C++ Engine (src/mint/)"]
        B1["bamboomint.cpp"]
        B2["bamboomint.h"]
        B3["bamboomintconfig.h"]
        B4["libbamboomint.so (build output)"]
    end

    subgraph Layer3["Tầng 3: Fcitx5 Framework"]
        C1["fcitx5 daemon"]
        C2["Input Context Manager"]
        C3["UI / Tray"]
    end

    A1 -->|"ParseInputMethod()"| A2
    A2 -->|"Buildmode C-Archive"| A4
    A4 -->|"Link"| B4
    B1 -->|"Register Addon"| C1
    C1 -->|"Key Events"| B1
    B1 -->|"Call Go funcs"| A4
    B1 -->|"Update UI"| C3
```

---

## 2. Cấu Trúc Các Kiểu Gõ Trong Go Core

### 2.1. Nơi định nghĩa

**File:** `bamboo/bamboo-core/input_method_def.go`

Đây là **nguồn duy nhất** định nghĩa các kiểu gõ. Có tổng cộng **9 kiểu gõ** được khai báo trong map `InputMethodDefinitions`:

```mermaid
graph LR
    IM["InputMethodDefinitions (map)"] --> T1["Telex"]
    IM --> T2["VNI"]
    IM --> T3["VIQR"]
    IM --> T4["Microsoft layout"]
    IM --> T5["Telex 2"]
    IM --> T6["Telex + VNI"]
    IM --> T7["Telex + VNI + VIQR"]
    IM --> T8["VNI Bàn phím tiếng Pháp"]
    IM --> T9["Telex W"]
```

### 2.2. Chi tiết từng kiểu gõ

| STT | Tên | Ý nghĩa | Ví dụ phím tắt |
|-----|-----|---------|----------------|
| 1 | **Telex** | Chuẩn Telex | `a`→â, `w`→ư/ơ/ă, `s`→sắc |
| 2 | **VNI** | Kiểu VNI số | `1`→sắc, `6`→â/ê/ô |
| 3 | **VIQR** | Kiểu VIQR dấu | `'`→sắc, `^`→â/ê/ô |
| 4 | **Microsoft layout** | Layout MS Office | `8`→sắc, `1`→ă, `2`→â |
| 5 | **Telex 2** | Telex mở rộng | Thêm `[]{}` cho ư/ơ/Ư/Ơ |
| 6 | **Telex + VNI** | Gộp Telex + VNI | `af` hoặc `a1` đều ra `à` |
| 7 | **Telex + VNI + VIQR** | Gộp cả 3 | `af`, `a1`, `a'` đều ra `à` |
| 8 | **VNI Bàn phím tiếng Pháp** | VNI cho bàn phím AZERTY Pháp | `é`→sắc, `è`→â/ê/ô |
| 9 | **Telex W** | Telex variant W | Tương tự Telex 2 |

### 2.3. Cách Go parse kiểu gõ

```mermaid
sequenceDiagram
    participant Cpp as C++ Engine
    participant Go as Go Core
    participant IM as InputMethodDefinitions

    Cpp->>Go: GetInputMethodNames()
    Go->>IM: Lặp qua map keys
    IM-->>Go: ["Telex","VNI","VIQR",...]
    Go-->>Cpp: Trả về char** array

    Cpp->>Go: NewEngine("Telex", dict, macro)
    Go->>IM: ParseInputMethod(IMDefs, "Telex")
    IM-->>Go: InputMethod object (rules)
    Go->>Go: BambooEngine{inputMethod: object}
    Go-->>Cpp: Trả về engine handle
```

---

## 3. Luồng Dữ Liệu Khi Người Dùng Gõ Phím

```mermaid
sequenceDiagram
    actor User
    participant App as Ứng dụng (Text Editor)
    participant F5 as Fcitx5 Daemon
    participant Addon as libbamboomint.so
    participant Go as bamboo-core.a
    participant Dict as vietnamese.cm.dict

    User->>App: Nhấn phím 'a'
    App->>F5: Key Event (Forward)
    F5->>Addon: keyEvent(entry, keyEvent)
    Addon->>Go: EngineProcessKeyEvent(handle, keyCode, mask)
    Go->>Go: BambooEngine.ProcessKey('a', VietnameseMode)
    Go->>Go: Áp dụng rules từ InputMethod đã parse
    Go->>Dict: Kiểm tra spell check (nếu bật)
    Dict-->>Go: isValid?
    Go-->>Addon: Trả về preedit / commit text
    Addon->>F5: Cập nhật InputPanel (preedit)
    F5->>App: Hiển thị tiếng Việt đã gõ
```

---

## 4. C++ Engine Khởi Tạo Như Thế Nào?

### 4.1. Constructor `BambooMintEngine`

```mermaid
flowchart TD
    A[BambooMintEngine Constructor] --> B["GetInputMethodNames() từ Go"]
    B --> C["Thêm 'Custom' vào cuối danh sách"]
    C --> D["Lưu vào imNames_"]
    D --> E{"Kiểm tra 'Telex' có tồn tại?"}
    E -->|Không| F["Throw runtime_error"]
    E -->|Có| G["config_.inputMethod.setList(imNames_)"]
    G --> H["Load từ điển vietnamese.cm.dict"]
    H --> I["Tạo các UI Actions"]
    I --> J["Đăng ký với fcitx5"]

    K["reloadConfig()"] --> L["Đọc conf/bamboomint.conf"]
    L --> M["Với mỗi imName trong imNames_"]
    M --> N["Đọc macro table từ conf/bamboomint-macro-<imName>.conf"]
    N --> O["Tạo macro table object"]
```

### 4.2. Tạo engine thực tế trong `BambooMintState::setEngine()`

```mermaid
flowchart TD
    A[BambooMintState::setEngine] --> B{"Chọn IM gì?"}
    B -->|"Custom"| C["Lấy custom keymap từ config"]
    C --> D["NewCustomEngine(keymap_array, dict, macro)"]
    D --> E["Go: Parse 'Custom' IM từ keymap"]

    B -->|"Telex / VNI / ..."| F["NewEngine(imName, dict, macroTable[imName])"]
    F --> G["Go: Parse IM từ InputMethodDefinitions"]

    E --> H[BambooEngine Go]
    G --> H
```

---

## 5. Liên Kết Với Macro Table và Custom Keymap

```mermaid
flowchart LR
    subgraph Config["Config Files"]
        C1["conf/bamboomint.conf"]
        C2["conf/bamboomint-custom-keymap.conf"]
        C3["conf/bamboomint-macro-Telex.conf"]
        C4["conf/bamboomint-macro-VNI.conf"]
        C5["conf/bamboomint-macro-VIQR.conf"]
        C6["..."]
    end

    subgraph Engine["C++ Engine"]
        E1["BambooMintConfig"]
        E2["BambooMintCustomKeymap"]
        E3["macroTables_['Telex']"]
        E4["macroTables_['VNI']"]
        E5["macroTables_['VIQR']"]
        E6["..."]
    end

    subgraph GoLayer["Go Core"]
        G1["MacroTable"]
        G2["InputMethod 'Telex'"]
        G3["InputMethod 'VNI'"]
        G4["..."]
    end

    C1 --> E1
    C2 --> E2
    C3 --> E3
    C4 --> E4
    C5 --> E5

    E3 -->|"newMacroTable()"| G1
    E4 -->|"newMacroTable()"| G1

    E1 -->|"inputMethod='VNI'"| E4
    E1 -->|"inputMethod='Telex'"| E3

    E3 -->|"NewEngine(..., macroTable)"| G2
    E4 -->|"NewEngine(..., macroTable)"| G3
```

---

## 6. Đánh Giá Ảnh Hưởng Nếu Xóa Các Kiểu Gõ (Chỉ Giữ Telex)

### 6.1. Phân tích từng file cần sửa

| File | Dòng cần sửa | Mức độ ảnh hưởng |
|------|-------------|-----------------|
| `bamboo/bamboo-core/input_method_def.go` | Xóa 8 entries khỏi `InputMethodDefinitions`, chỉ giữ `"Telex"` | **Cao** — Nguồn duy nhất định nghĩa IM |
| `bamboo/bamboo-core/bamboo_test.go` | Xóa các test dùng VNI, VIQR, Microsoft layout, Telex 2, v.v. | **Trung bình** — Chỉ ảnh hưởng test |
| `src/mint/bamboomint.cpp` | Dòng 235-238: Kiểm tra `"Telex"` tồn tại → **giữ nguyên** | **Thấp** — Không cần sửa, vẫn hợp lệ |
| `src/bamboo.cpp` | Tương tự trên — kiểm tra `"Telex"` | **Thấp** — Không cần sửa |
| `src/mint/bamboomintconfig.h` | Không cần sửa — dropdown list tự động từ `imNames_` | **Thấp** |
| Các file `conf/bamboomint-macro-*.conf` | Các file config cũ sẽ không còn được đọc | **Thấp** — Chỉ là orphan files |

### 6.2. Sơ đồ impact toàn cục

```mermaid
flowchart TD
    A["Quyết định: Xóa tất cả IM, chỉ giữ Telex"] --> B["Sửa Go Core"]
    A --> C["Sửa Go Tests"]
    A --> D["Dọn dẹp config"]

    B --> B1["input_method_def.go:<br/>Xóa 8 entries map"]
    B --> B2["bamboo-c.go:<br/>Không cần sửa — GetInputMethodNames()<br/>tự động lặp map động"]
    B --> B3["rules_parser.go:<br/>Không cần sửa — parser generic"]

    C --> C1["bamboo_test.go:<br/>Xóa test VNI, VIQR, MS layout,<br/>Telex 2, Telex+VNI, Telex+VNI+VIQR,<br/>VNI Pháp, Telex W"]

    D --> D1["~/.config/fcitx5/conf/bamboomint.conf:<br/>Nếu đang chọn IM bị xóa,<br/>fcitx5 sẽ fallback về Telex hoặc crash"]
    D --> D2["~/.config/fcitx5/conf/bamboomint-macro-*.conf:<br/>8 file macro của IM bị xóa thành orphan"]
    D --> D3["~/.config/fcitx5/profile:<br/>Nếu DefaultIM=mint và mint đang chọn<br/>IM bị xóa, cần reset về Telex"]
```

### 6.3. Rủi ro cần lưu ý

| Rủi ro | Mô tả | Cách tránh |
|--------|-------|-----------|
| **Crash khởi động** | Nếu `~/.config/fcitx5/conf/bamboomint.conf` lưu `InputMethod=VNI` trong khi VNI đã bị xóa khỏi Go map, engine C++ vẫn cố gọi `NewEngine("VNI",...)` và Go không tìm thấy → crash | Thêm validation trong C++: nếu IM không tồn tại trong list, fallback về `"Telex"` |
| **Config orphan** | 8 file macro `.conf` cũ chiếm dung lượng (nhỏ) | Không cần xóa thủ công, chỉ cần bỏ qua |
| **Tests Go lỗi** | `bamboo_test.go` có ~20 test dùng các IM bị xóa | Xóa hoặc comment các test đó |
| **Người dùng khác** | Nếu đã cài addon này trên máy khác, họ đang dùng VNI → sau update sẽ bị lỗi | Thêm fallback Telex trong `NewEngine()` hoặc migration logic |

---

## 7. Sơ Đồ Quyết Định: Có Nên Xóa Không?

```mermaid
flowchart TD
    Start["Muốn xóa các IM không phải Telex?"] --> Q1["Có cần hỗ trợ VNI/VIQR/... không?"]
    Q1 -->|"Không, chỉ dùng Telex"| Q2["Có cần backward compatibility không?"]
    Q1 -->|"Có"| Keep["Giữ nguyên tất cả IM"]

    Q2 -->|"Không, chỉ dùng local"| Q3["Chấp nhận sửa code + test không?"]
    Q2 -->|"Có, cần share cho người khác"| Keep

    Q3 -->|"Có"| Action["XÓA:<br/>1. Sửa input_method_def.go<br/>2. Dọn tests<br/>3. Thêm fallback Telex<br/>4. Build + test"]
    Q3 -->|"Không, ngại sửa nhiều"| Keep

    Action --> End["Chỉ còn Telex (+ Custom)"]
    Keep --> End2["Giữ nguyên 9 IM"]
```

---

## 8. Tóm Tắt Các Bước Thực Hiện Nếu Xóa

### Bước 1: Sửa Go Core

```go
// File: bamboo/bamboo-core/input_method_def.go
// Trước: 9 entries
// Sau: Chỉ giữ Telex

var InputMethodDefinitions = map[string]InputMethodDefinition{
    "Telex": {
        "z": "XoaDauThanh",
        "s": "DauSac",
        "f": "DauHuyen",
        "r": "DauHoi",
        "x": "DauNga",
        "j": "DauNang",
        "a": "A_Â",
        "e": "E_Ê",
        "o": "O_Ô",
        "w": "UOA_ƯƠĂ",
        "d": "D_Đ",
    },
}
```

### Bước 2: Dọn tests

Xóa hoặc comment các test trong `bamboo_test.go` dùng: VNI, VIQR, Microsoft layout, Telex 2, Telex+VNI, Telex+VNI+VIQR, VNI Bàn phím tiếng Pháp, Telex W.

### Bước 3: (Tùy chọn) Thêm fallback trong C++

Trong `bamboomint.cpp`, trước khi gọi `NewEngine()`, kiểm tra nếu tên IM không tồn tại thì fallback về `"Telex"`.

### Bước 4: Build + Test

```bash
cd build && make -j4
sudo cp src/mint/libbamboomint.so /usr/lib/x86_64-linux-gnu/fcitx5/
fcitx5-remote -r
```

---

## 9. Kết Luận

- Các kiểu gõ được **định nghĩa tập trung** trong `bamboo/bamboo-core/input_method_def.go`.
- C++ engine **không hardcode** danh sách IM; nó đọc động từ Go qua `GetInputMethodNames()`.
- Nếu xóa các IM không phải Telex, **chỉ cần sửa 1 file Go** là đủ về mặt logic. Nhưng cần dọn tests và xử lý config cũ.
- **Rủi ro chính:** Người dùng đang có config chọn IM bị xóa (ví dụ `InputMethod=VNI`) sẽ gặp lỗi khi engine cố khởi tạo. Cần thêm fallback Telex trong C++.
